let personName: string[] = [' Shayan', ' Noman', ' Saba', ' Ayesha', ' Ali'];
let message: string= "Tomorrow is the Birthday of: "
// Using a traditional for loop
for (let i = 0; i < personName.length; i++) {
    console.log(message + personName[i]);
}